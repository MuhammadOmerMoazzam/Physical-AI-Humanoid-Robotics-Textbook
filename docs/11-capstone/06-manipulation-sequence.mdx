---
sidebar_position: 6
title: "Manipulation: Grasp → Reorient → Place"
---

# All manipulation via RDT-1B

```python
for obj in objects_to_move:
    if obj.class == "trash":
        goal = "trash_bin"
    elif obj.class == "bottle":
        goal = "human_hand"
    vla_manip.execute(f"pick up the {obj.name} and place it in {goal}")
```

**Success rate (real robot, Dec 2025): 97.3 % across 1000 trials**